{
  "name": "toolforge",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:portal": "pnpm --filter portal dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
