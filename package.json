{
  "name": "toolforge",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:portal": "pnpm --filter portal dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  }
}
